module Main
{
  let input;
  input = 10;

  function factorial(n)
  {
    let total;
    total = 1;
    while (n > 1)
    {
      total = total * n;
      n = n - 1;
    }
    return total;
  }

  return factorial(input);
}
