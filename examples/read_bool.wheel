module Main
{
  import readBool from StdReader;
  import printBool, printNum from Native;
  
  let b = readBool();
  if (b.isValid)
  {
    return printBool(b.value);
  }
  else
  {
    return printNum(0);
  }
}